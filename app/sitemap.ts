import type { MetadataRoute } from "next"; import { guides } from "@/lib/editorial-content";
export default function sitemap(): MetadataRoute.Sitemap { const base = "https://cripto-pulso.vercel.app"; const pages = ["", "/guias", "/nosotros", "/metodologia", "/privacidad", "/terminos", "/contacto"]; return [...pages.map((path) => ({ url: `${base}${path}`, lastModified: new Date("2026-09-02"), changeFrequency: path === "" ? "daily" as const : "monthly" as const, priority: path === "" ? 1 : .7 })), ...guides.map((guide) => ({ url: `${base}/guias/${guide.slug}`, lastModified: new Date("2026-09-02"), changeFrequency: "monthly" as const, priority: .8 }))]; }
