import type { Metadata } from "next"; import Link from "next/link"; import EditorialShell from "@/app/components/EditorialShell"; import { guides } from "@/lib/editorial-content";
export const metadata: Metadata = { title: "Guías y análisis", description: "Guías originales para interpretar información económica, combustibles y criptomonedas en Bolivia.", alternates: { canonical: "/guias" } };
export default function GuidesPage() { return <EditorialShell><section className="editorial-hero"><span>CONTENIDO ORIGINAL · CRIPTOPULSO</span><h1>Guías para entender los datos y decidir con más cuidado</h1><p>Explicamos qué mide cada herramienta, cómo interpretar sus límites y qué preguntas conviene hacer antes de actuar.</p></section><section className="guide-grid">{guides.map((guide) => <article key={guide.slug}><small>{guide.readingMinutes} MIN DE LECTURA</small><h2>{guide.title}</h2><p>{guide.description}</p><Link href={`/guias/${guide.slug}`}>Leer guía completa →</Link></article>)}</section></EditorialShell>; }
